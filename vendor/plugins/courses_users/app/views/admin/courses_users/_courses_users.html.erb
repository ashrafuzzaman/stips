<li class='clearfix record <%= cycle("on", "on-hover") %>' id="<%= dom_id(courses_users) -%>">
  <span class='title'>
    <%=h courses_users.first_name %> &nbsp;<%=h courses_users.last_name %>
    <%= "<span style=color:#{courses_users.status_color};> #{courses_users.status}</span>" %> &nbsp;
    <strong>Confirmed at</strong> <%=h courses_users.confirmed_at %>
    <strong>Paid</strong> <%=h courses_users.amount_paid %>
    <span class="preview">&nbsp;</span>
  </span>
</li>
